{
  "name": "@acme/fastify-crud",
  "version": "0.1.0",
  "main": "./src/server.ts",
  "license": "MIT",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "bun build src/server.ts --outdir ./build --sourcemap=external --minify --target node",
    "start": "bun build/server.js",
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint . --ext .ts,.tsx",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@acme/db": "*",
    "@acme/shared-functions": "*",
    "@clerk/clerk-js": "^4.67.0",
    "@clerk/fastify": "^0.6.31",
    "@trpc/client": "^10.43.1",
    "@trpc/next": "^10.43.1",
    "@trpc/server": "^10.43.1",
    "@types/node": "^20.11.5",
    "fastify": "^4.24.3",
    "superjson": "^1.9.1",
    "zod": "^3.18.0"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "bun": ">=1.0.22"
  }
}
