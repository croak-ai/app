{
  "name": "@acme/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "with-env": "dotenv -e ../../.env --",
    "push": "drizzle-kit push:sqlite",
    "migrate": "pnpm ts-node migrate.ts",
    "generate": "drizzle-kit generate:sqlite"
  },
  "dependencies": {
    "@acme/shared-functions": "*",
    "@libsql/client": "0.4.0-pre.5",
    "@planetscale/database": "^1.10.0",
    "drizzle-orm": "^0.28.6",
    "znv": "^0.4.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "drizzle-kit": "^0.19.13",
    "ts-node": "^10.9.2",
    "typescript": "^5.1.6"
  }
}
